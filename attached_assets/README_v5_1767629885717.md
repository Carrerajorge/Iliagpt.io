# 🚀 Agente IA v5.0 - Enterprise Edition

## Archivos Incluidos

1. **agente_ia_v5.py** (110KB) - Agente principal con todas las funcionalidades
2. **service_api.py** - Servicio FastAPI para exponer el agente como API REST
3. **run_service.py** - Script para iniciar el servicio

## Instalación en Replit

1. Crea un nuevo Repl Python 3.10+
2. Sube los 3 archivos
3. Renombra `agente_ia_v5.py` a mantener ese nombre
4. Ejecuta: `python run_service.py`

## Uso Interactivo

```bash
python agente_ia_v5.py
```

Selecciona opción 1 para modo interactivo.

## Uso como API

```bash
python service_api.py
```

El servidor estará en http://localhost:8081

### Endpoints Disponibles

| Método | Endpoint | Descripción |
|--------|----------|-------------|
| GET | `/` | Info del servicio |
| GET | `/health` | Health check |
| GET | `/status` | Estado del agente |
| GET | `/tools` | Listar herramientas |
| POST | `/run` | Ejecutar agente |
| POST | `/browse` | Navegar URL |
| POST | `/search` | Búsqueda web |
| POST | `/document` | Crear documento |
| POST | `/execute` | Ejecutar herramienta |
| GET | `/files` | Listar archivos creados |
| GET | `/files/{name}` | Descargar archivo |

### Ejemplos de API

```bash
# Ejecutar agente
curl -X POST http://localhost:8081/run \
  -H "Content-Type: application/json" \
  -d '{"message": "Crea una presentación sobre IA"}'

# Búsqueda
curl -X POST http://localhost:8081/search \
  -H "Content-Type: application/json" \
  -d '{"query": "Python programming", "num_results": 5}'

# Navegar
curl -X POST http://localhost:8081/browse \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com", "screenshot": true}'

# Crear documento
curl -X POST http://localhost:8081/document \
  -H "Content-Type: application/json" \
  -d '{
    "doc_type": "pptx",
    "title": "Mi Presentación",
    "content": [
      {"title": "Intro", "content": "Bienvenidos"},
      {"title": "Puntos", "bullets": ["Punto 1", "Punto 2"]}
    ]
  }'
```

## Capacidades

- 🌐 **Navegador Virtual** - Playwright/Selenium
- 📸 **Screenshots** automáticos
- 📊 **Documentos** - PPTX, DOCX, XLSX
- 🔍 **Búsqueda** web en tiempo real
- 💾 **Caché** multinivel
- ⏱️ **Rate Limiting**
- 8 herramientas: shell, file, python, search, browser, document, message, research

## Notas

- Los documentos se crean en `workspace_v5/output/`
- Los screenshots en `workspace_v5/screenshots/`
- El caché en `workspace_v5/.cache/`
