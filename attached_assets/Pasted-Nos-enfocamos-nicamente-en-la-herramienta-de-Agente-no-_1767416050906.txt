Nos enfocamos únicamente en la herramienta de Agente (no tocar chat/UI general ni romper endpoints): al seleccionar Agente NO debe abrirse un panel/pantalla lateral; en su lugar, el Agente debe comportarse como en Manus: el usuario envía su mensaje en el chat normal y ahí mismo aparece un mensaje/runcard inline del Agente que va actualizándose paso a paso con un feed tipo Action/Observation + barra de progreso + estado (“Planificando…”, “Ejecutando…”, “Verificando…”), y al final entrega la respuesta final + artefactos + citas; Manus enfatiza transparencia y control con registro detallado de acciones y un “replay”/auditoría de lo que hizo el agente, además de autorización explícita cuando opera navegador local y monitoreo en tiempo real  ￼, y describe un agente que planifica y ejecuta flujos multi-paso entregando resultados completos  ￼; implementa entonces: (1) AgentInlineRenderer dentro del message bubble que renderice eventos en UI humana (NO JSON crudo), con agrupación por step, contador real de pasos (evitar el bug “0/0 pasos”), y secciones colapsables “Plan / Progreso / Artefactos / (Computer opcional en modal)”; (2) streaming SSE/polling de agent_events para ir avisando al usuario cada decisión/acción, manteniendo un “audit trail” (event sourcing) como en Manus  ￼; (3) corregir el error al obtener respuesta: maneja estados queued/planning/running/verifying/done/failed con UI consistente, retries visuales, y fallback si falta plan/steps (nunca mostrar undefined/null ni romper render); (4) ejecución larga: el run debe poder trabajar 60 minutos continuos sin bloquear UI (worker asíncrono + heartbeats + rehidratación) y el chat solo consume eventos/estado; añade npm run agent:soak:60 (3600s) que valide estabilidad (memoria/FDs/contextos browser) y success rate, guardando outputs en test_results/agent_ui_soak_60m_*; (5) paridad de UX: “trabajando…” mientras ejecuta, “replay” completo al terminar, botones inline (cancel/pausa/reintento) y notificación al finalizar; entrega evidencia: lista exacta de archivos tocados + screenshots/gif del flujo final + outputs de tests/E2E que prueben que seleccionar Agente ya no abre panel lateral y que el progreso paso-a-paso se renderiza inline sin errores.