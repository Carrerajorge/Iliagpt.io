Integra y mejora únicamente el módulo del Agente (sin modificar la arquitectura/UI del chat ni romper endpoints actuales) para que tenga capacidades tipo Manus: ejecución asíncrona de ‘runs’ que continúen aunque el usuario cierre la vista, con notificación al completar; orquestación multi-rol (PlannerAgent→ExecutorAgent→VerifierAgent) con planificación explícita y reproducción paso-a-paso (timeline) mostrando acciones y herramientas usadas; motor de herramientas con ToolRegistry y PolicyEngine (deny-by-default + human-in-the-loop para acciones sensibles) que soporte: Web research (search→browse→extract→cite), web automation con Playwright cloud browser y opcional ‘local browser operator’ estilo conector (modo seguro que usa sesión del usuario con autorización explícita y logging de clicks/inputs), code execution en sandbox (sin red por defecto, límites CPU/RAM/timeout, allowlist de libs), y document/data workflows (Excel multi-hoja/CSV/PDF/DOCX) generando artefactos (reportes, tablas, gráficos, archivos) y devolviendo un paquete estándar {final_answer, artifacts[], citations[], run_log, metrics}; implementa persistencia y auditoría en DB con agent_runs/agent_steps/agent_events (append-only) con correlationId, idempotency keys, retries con backoff+jitter, circuit breakers por tool/dominio, cancel/pause/resume/retry reales propagando cancel tokens; obliga a respuestas con citas verificables (sources con quote corto + locator) y verificación automática de cobertura (si hay claims sin cita, el Verifier re-busca fuentes); añade observabilidad (logs estructurados + métricas p95/p99 por fase y por tool) y un comando npm run agent:certify que ejecute unit+integration+E2E+bench+soak/chaos, guarde outputs en test_results/, y produzca replit.md ‘Agent Production Readiness’ con checklist 100% ✅; objetivo: que al presionar ‘Agente’ se ejecute un run end-to-end que planifique, use herramientas (incluyendo navegación web rápida y automatización), se autocorrija con verificación, y entregue resultados/artefactos listos para usar, manteniendo el chat normal intacto.